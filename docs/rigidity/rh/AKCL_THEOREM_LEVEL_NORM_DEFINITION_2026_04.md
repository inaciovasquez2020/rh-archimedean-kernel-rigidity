# AKCL Theorem-Level Norm Definition (2026-04)

## Status
OPEN

## Target object
Define the exact norm
\[
\|\cdot\|_{\mathcal A_{\mathrm{AKCL}}}
\]
appearing in
`docs/rigidity/rh/AKCL_BASIS_TO_CLASS_DENSITY_LEMMA_2026_04.md`
and required by
`docs/rigidity/rh/AKCL_TRANSFER_LEMMA_FINISH_CONDITION_2026_04.md`.

## Required definition shape
The norm definition must specify all of the following:

1. Underlying vector space.
2. Exact formula.
3. Finiteness domain.
4. Interaction with the neutral-mode constraint.
5. Compatibility with the theorem-level energy functional.
6. Completion rule defining the closed admissible class.

## Pending exact literals
Replace this section by an exact formula of the form
\[
\|f\|_{\mathcal A_{\mathrm{AKCL}}}
=
\text{explicit expression in } f .
\]

If the theorem-level topology is not generated by a norm, replace the symbol by the exact seminorm / graph norm / Hilbert norm actually used and update every downstream file accordingly.

## Role in the chain
This file supplies Missing Object 1 from
`docs/rigidity/rh/AKCL_BASIS_TO_CLASS_DENSITY_LEMMA_2026_04.md`.

## Finish condition
Replace `OPEN` by `PROVED` only after the repository contains an exact formula-level norm definition usable by the density lemma and the transfer lemma.
