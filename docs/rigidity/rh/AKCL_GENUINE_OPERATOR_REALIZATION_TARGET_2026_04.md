# AKCL Genuine Operator Realization Target (2026-04)

## Status
OPEN

## Exact target
Construct theorem-level operators or quadratic forms
\[
E_{\mathrm{AKCL}},
\qquad
D_{\mathrm{AKCL}}
\]
on a common dense domain in \(H_{\mathrm{AKCL}}\).

## Required realization identities
For every declared packet family and its embedding \(J_\theta\),
\[
(M_E^{(\theta)})_{ij}
=
\langle J_\theta e_i,\ E_{\mathrm{AKCL}} J_\theta e_j\rangle_{H_{\mathrm{AKCL}}},
\]
\[
(M_D^{(\theta)})_{ij}
=
\langle J_\theta e_i,\ D_{\mathrm{AKCL}} J_\theta e_j\rangle_{H_{\mathrm{AKCL}}}.
\]

## Exact missing objects
1. common dense operator domain;
2. exact operator formulas;
3. symmetry / positivity data;
4. finite-compression proof.

## Finish condition
Replace `OPEN` by `PROVED` only after \(M_E^{(\theta)}\) and \(M_D^{(\theta)}\) are realized as finite compressions of theorem-level quadratic forms for all declared packet families.
