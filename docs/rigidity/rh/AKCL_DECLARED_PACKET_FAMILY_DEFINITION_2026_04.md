# AKCL Declared Packet Family Definition (2026-04)

## Status
OPEN

## Target object
Define the exact declared packet family union
\[
\mathcal P_{\mathrm{AKCL}}
\]
appearing in
`docs/rigidity/rh/AKCL_BASIS_TO_CLASS_DENSITY_LEMMA_2026_04.md`.

## Required definition shape
The definition must specify all of the following:

1. Admissible parameter set.
2. Packet centers.
3. Packet width rule.
4. Neutral-mode projection rule.
5. Orthonormalization rule.
6. Union rule over parameter choices.

## Pending exact literals
Replace this section by an exact formula of the form
\[
\mathcal P_{\mathrm{AKCL}}
=
\bigcup_{\theta\in\Theta_{\mathrm{AKCL}}}
\operatorname{span}\{b^{(\theta)}_1,\dots,b^{(\theta)}_{m(\theta)}\},
\]
where each packet basis element \(b^{(\theta)}_r\) is given by an explicit formula.

## Role in the chain
This file supplies Missing Object 3 from
`docs/rigidity/rh/AKCL_BASIS_TO_CLASS_DENSITY_LEMMA_2026_04.md`.

## Finish condition
Replace `OPEN` by `PROVED` only after the repository contains an exact formula-level definition of
\(\mathcal P_{\mathrm{AKCL}}\) compatible with the declared surrogate basis construction.
